CREATE TABLE user_table (
id NUMBER(10) NOT NULL,
email varchar(255),
password varchar(255),
phoneNumber varchar(255),
nickname varchar(255),
point Number(10),
provider varchar(255),
providerId varchar(255),
role varchar(255),
username varchar(255),
CONSTRAINT id PRIMARY KEY (id)
);

-- 자동으로 설정되는 id를 위한 시퀀스
CREATE SEQUENCE USER_SEQ START WITH 1 INCREMENT BY 1 MAXVALUE 99999999 NOCYCLE NOCACHE;

commit;


/* 설명
user_table의 컬럼
1. id => 회원가입 시 자동으로 설정되는 고유한 id, primary key, user_seq 시퀀스를 통해 자동 증가됨
2. email 
3. password => 직접 회원가입한 경우는 입력한 비밀번호가 저장됨, 반면 SNS 로그인 시 임의의 값이 자동으로 설정됨(SNS를 통해 간접적으로 로그인 되므로 굳이 비밀번호를 저장할 필요가 없다)
4. username => 직접 회원가입한 경우는 입력한 username(로그인 id)rk 저장됨, 반면 SNS 로그인 시 임의의 값이 자동으로 설정됨(SNS를 통해 간접적으로 로그인 되므로 굳이 아이디를 저장할 필요가 없다)


*/
